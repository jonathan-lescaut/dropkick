{% extends 'base.html.twig' %}

{% block title %}Cart index{% endblock %}

{% block body %}
    <h1>Cart index</h1>
<section class="navbarMargin">
    <table class="table">
        <thead>
            <tr>
                <th>Numéro Commande</th>
                <th>Contenu</th>
                <th>Client</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>

        
        {% for cart in carts %}

            <tr>
                <td>{{ cart.id }}</td>
                <td>{{ cart.contentCart }}</td>
                <td>{{ cart.userCart.email }}</td>
                <td>
                    <a href="{{ path('cart_order_edit', {'id': cart.id}) }}">Modifier</a>
                </td>
                <td>
                   {{ include('cart_order/_delete_form.html.twig') }}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    </section>

{% endblock %}
