{% extends 'admin.html.twig' %}

{% block title %}Edit Products{% endblock %}

{% block body %}
<section class="pageProduct navbarMargin">
    <h1>Edit Products</h1>

    {{ include('products/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('productsAdmin_index') }}" class="btn btn-primary">Retour liste</a>
</section>
{% endblock %}
