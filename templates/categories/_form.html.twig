{{ form_start(form) }}
    <div class="form-group row">
        <label class="col-sm-2 col-form-label">Le nom de la catégorie</label>
        <div class="col-sm-10">
            {{ form_widget(form.nameCategory) }}
        </div>
    </div>

    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
