{% extends 'base.html.twig' %}

{% block title %}Menus{% endblock %}

{% block body %}
<section class="pageMenu navbarMargin">

    <h1>Menus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>NameMenu</th>
                <td>{{ menu.nameMenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('menus_index') }}">back to list</a>

    <a href="{{ path('menus_edit', {'id': menu.id}) }}">edit</a>

    {{ include('menus/_delete_form.html.twig') }}
</section>

{% endblock %}
